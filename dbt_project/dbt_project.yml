name: 'hermosillo_real_estate'
version: '1.0.0'

profile: 'my_dbt_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  hermosillo_real_estate:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
